import { run } from 'runme'

async function runCommand() {
    const result = await run(['carl'], )
    return result
  }

//console.log(result.stdout) // outputs: { exitCode: 0, stdout: 'Hello World\r\n', stderr: '' }